# Translation Work Log - huxhold-et-al-2014

## Pass 1 - 제목, 구조화 초록, 용어

- 승인된 `full.md`와 `source_segments.json`만을 권위 원문으로 삼아 제목, 저자·소속, 출판정보, 구조화 초록, 핵심어를 번역했다.
- `subjective well-being`은 `주관적 안녕`, `positive affect`와 `negative affect`는 각각 `긍정 정서`와 `부정 정서`로 통일하고, 최초 정의 뒤에는 원문의 약어 `SWB`, `PA`, `NA`를 유지했다.
- `informal social activities`는 `비공식 사회활동`, `family members`는 `가족 구성원`, `friends`는 `친구`, `life satisfaction`은 `삶의 만족도`로 일관되게 옮겼다.
- 초록의 `may` 두 곳을 가능성 표현으로 보존해, 친구 활동의 효과가 노년기에 더 중요해질 수 있고 노화의 부정적 효과를 완충할 수 있다는 저자 수준 이상의 단정을 피했다.

## Pass 2 - 이론, 가설, 표본과 분석방법

- 사회정서적 선택성이론, 강점과 취약성 모형, 친구관계와 가족관계의 구조적·기능적 차이, 세 가설의 논리와 연령집단 대비를 원문 순서대로 번역했다.
- DEAS의 모집단 기반 대표표집, 코호트 순차설계, 1996·2002·2008년 파동, 최종 표본과 종단 참여자 수, 탈락 선택성 및 FIML 처리를 모두 보존했다.
- 삶의 만족도·PA·NA 척도의 문항 수와 응답범위, `Cronbach’s α T1/T2 = 0.84/0.85`, `0.87/0.99`, 건강 척도의 `0.94/0.93`을 원문 그대로 유지했다.
- `latent change score (LCS)`는 `잠재변화점수(LCS)`, `measurement invariance`는 `측정불변성`, `nested model comparisons`는 `내포모형 비교`로 통일했다.
- 변화 간 연관성은 단지 횡단적이며 논의에서 해석하지 않는다는 경고와, T1 활동 수준이 이후 안녕 변화와 맺는 연관성에 초점을 둔다는 분석 범위를 보존했다.

## Pass 3 - 결과, 그림과 표

- 연령집단·활동 상대·시점의 차이를 검정한 `Δχ² = 366.97`, `10.45`, `0.16`, `7.60`과 자유도·유의성 부호를 대조했다.
- 최종 LCS 모형의 `χ² = 809.59`, `df = 223`, `RMSEA = 0.032`, `CFI = 0.98` 및 측정불변성의 `CFI` 감소 `0.01 이하`를 보존했다.
- 중년 집단에서 PA와 삶의 만족도는 각각 `β = 0.08`, NA는 `β = 0.01`이라는 결과와 친구/가족 간 효과 차이가 유의하지 않았다는 대비를 유지했다.
- 노년 집단에서 PA의 `β = 0.09`, 삶의 만족도의 `βfriends = 0.14`와 `βfamily = 0.02`, NA의 상반된 `βfriends = −0.08`과 `βfamily = 0.08` 및 관련 `Δχ²`를 모두 확인했다.
- 그림 1, 그림 2, 회전된 표 1의 자산 경로를 그대로 두고 캡션과 접근성 설명을 완역했다.

## Pass 4 - 논의, 한계, 결론, 참고문헌

- 세 가설이 각각 부분적으로만 확인되었다는 결론과, 중년기에는 활동 상대가 중요하지 않지만 노년기에는 삶의 만족도와 NA에서 친구/가족 차이가 나타났다는 제한된 주장을 보존했다.
- 노년 집단에서 가족 활동은 NA 증가 또는 유지와, 친구 활동은 NA 감소와 연관되었다는 반대 방향의 결과를 논의와 결론에서도 일관되게 유지했다.
- 자원 중심 설명, 정서적으로 추동된 선별, 가족의 지원 역할 변화는 모두 저자의 `may`, `might`, `seem`, `plausible`, `speculate`에 맞추어 가능성 수준으로 번역했다.
- 자기보고와 회상 편향, 6년 간격의 두 측정 시점, 단기 정서 변동, 가족관계 유형과 관계 질을 구분하지 못한 한계를 빠짐없이 옮겼다.
- 연구비와 교신정보를 번역하고, 참고문헌 53개는 원문의 인쇄상 표기·구두점·DOI를 현대화하지 않은 채 그대로 보존했다. 줄바꿈 정규화 뒤 참고문헌 본문은 `full.md`와 정확히 일치한다.

## Pass 5 - 구조 정렬과 검증

- 22개 번역 세그먼트가 22개 원문 세그먼트와 동일한 ID와 순서로 대응하고, 원문·번역의 111개 마크다운 콘텐츠 블록과 132개 문서 블록 유형·순서가 일치한다.
- 펼쳐보기 대상 100개 문단/맥락 블록을 1:1로 정렬하고 264개 문장쌍을 의미, 순서, 누락, 인용, 숫자, 통계 부호, 한정 표현 기준으로 전수 검토했다.
- 그림 2의 `T1 vs. T2` 약어에서 생긴 자동 문장분리 오류를 수동으로 재결합했다. 최종 모든 정렬 항목은 `human-reviewed-v1`, 모든 문장쌍은 `verified` 상태이며 양쪽 본문을 완전한 순서로 덮는다.
- 문장쌍별 원문 숫자 누락은 0개이고, 참고문헌은 53/53개가 정확히 일치한다. strict alignment는 22/22 세그먼트에서 오류·경고 없이 통과했다.
- source-only 콘텐츠 검증은 번역 5,540단어, 본문 문단 100개, 참고문헌 비율 1.0으로 `schema_pass`를 보고한다.
- 지시에 따라 수동 승인과 메타데이터 변경은 하지 않았다. 따라서 펼쳐보기 설정은 활성화하지 않았으며, 정렬 데이터 자체의 직접 검증에서 100개 블록과 264개 문장쌍이 통과했다.
- 공개 사이트 빌드, 배포, 커밋 및 후속 학습자료 작성은 수행하지 않았다. 번역·정렬 파일 외의 사용자 기능은 추가하지 않았다.
