# Translation QA Checklist

- Reading: `kalmijn-leopold-2019`
- Alignment status: **PASS**

- [x] 22개 원문 `segment_id`가 같은 순서의 22개 번역 세그먼트와 대응한다.
- [x] 번역 전용 또는 누락된 `segment_id`가 없다.
- [x] 원문과 번역은 각각 123개 콘텐츠 블록이며 제목·절·문단·그림 구조가 같다.
- [x] 펼쳐보기 대상 117개 문단을 1:1로 정렬했다.
- [x] 379개 문장쌍을 모두 `human-reviewed-v1` / `verified`로 전수 검토했다.
- [x] 문장쌍별 원문 숫자 누락은 0건이다.
- [x] 참고문헌 52개는 원문과 문자 단위로 동일하다.
- [x] 엄격 정렬 검사는 22/22 세그먼트에서 오류·경고 없이 통과했다.
- [x] 격리 미리보기에서 117/117 문단 및 379/379 문장 펼쳐보기가 통과했다.
- [x] 별도 감사자가 PDF 16쪽, 번역 71개 블록, 참고문헌 52개, 모든 정렬과 격리 UI를 전수 재검수했고 최종 finding 0건으로 PASS했다.

## Manual review notes

- 제목, 저자·소속, 구조화 초록, 핵심어, 본문, 주, 참고문헌을 원문 순서 그대로 옮겼다.
- `solidarity`는 `결속`, `kinkeeping`은 `친족유지`, `kinkeeper`는 `친족유지자`, `sibling contact`는 `형제자매 접촉`으로 일관되게 번역했다.
- 첫 번째 부모와 두 번째 부모의 사망을 구분하고 가설 1–6, 단기 전환효과와 장기 기간효과, 두 번째 부모 사망 뒤의 이차 기간효과를 보존했다.
- 표본 3,812명, 사건·통제 표본, 조사차수·유지율, 다이애드 수, 개인-연도 구성, 사망 전후 측정의 모호성과 민감도 분석을 원문과 대조했다.
- 접촉·갈등·지원 측정, `Cronbach’s α = .71–.74`, `r = .5`, 연령 중심화 45.5세를 보존했다.
- 고정효과, 군집-강건 표준오차, 조건부 로짓, 선형확률모형, 개체 내 변환, `khb(KHB)` 매개검정의 역할을 과장 없이 옮겼다.
- 표 3의 전환·기간·매개 계수와 표 4의 부(-)의 기간계수(`−0.243`, `−0.359`, `−0.015`, `−0.023`) 및 유의성 표현을 대조했다.
- 두 번째 부모 사망 뒤 접촉이 일시적으로 증가한 뒤 장기적으로 감소하고 갈등도 감소한다는 결과를, 관계의 `악화`가 아니라 `희미해짐`이라는 저자의 해석과 함께 유지했다.
- 생존 부모와의 결속이 형제자매 접촉을 촉진한다는 해석과 역방향의 대안적 해석을 배제할 수 없다는 한계를 모두 보존했다.
- 표 1–4와 그림 1–2의 자산 경로, 캡션, 주, 접근성 설명과 모든 보고 수치를 확인했다. 표 1의 인쇄상 잘못된 변수 설명과 본문의 인쇄된 `가설 4` 교차참조는 임의로 고치지 않았다.
- 독립감사에서 비의학적 갈등 맥락에 어색했던 `유병률`을 `갈등의 비율`로 고친 뒤, 같은 감사자가 변경 문장과 117/379 정렬·펼쳐보기를 다시 검사해 중대·실질·경미 finding 0건을 확인했다.
- 수동 승인, 공개 산출물 생성, 배포 및 커밋은 수행하지 않았다.
