# Translation QA Checklist

- Reading: `lee-yeung-2021`
- Author-side alignment status: **PASS**
- Workflow status: **HOLD — independent Stage 2 audit required**

- [x] 제목, 저자·소속, 저널·인용정보, 접수·결정일, 편집자, 교신, 저작권 메타데이터를 원문 순서대로 보존했다.
- [x] 구조화 초록, 핵심어, 본문 10개 절, 보충자료, 연구비 지원, 이해상충과 참고문헌을 요약으로 대체하지 않고 완역했다.
- [x] 승인 원문의 23개 `segment_id`와 블록 1–126을 같은 순서와 범위로 사용했으며, 번역도 23개 세그먼트와 126개 블록이다.
- [x] 참고문헌을 포함한 117개 문단을 단조롭게 1:1 정렬하고, 372개 문장쌍을 `human-reviewed-v1` / `verified`로 전수 검토했다.
- [x] 수동 문장경계 규칙 5개(`tr-033`, `tr-042`, `tr-046`, `tr-048`, `tr-060`)가 양쪽 문장을 누락 없이 순서대로 덮으며 문장쌍별 원문 숫자 누락은 0건이다.
- [x] PDF 14쪽(인쇄면 642–655쪽)을 원해상도로 확인하고, 통계분석의 두 식과 `a = 노동시장 진입`, `b = 노동시장 이탈` 방향을 그대로 옮겼다.
- [x] KLoSA 2006–2016, 기초연령 50–64세, 2016년 60–74세, 유급노동 무경험 제외 `n = 1,295`, 노동상태 결측 탈락 14.3%, 관측손실 중 사망 약 14%를 보존했다.
- [x] 최종 표본 2,600명(남성 1,579명, 여성 1,021명), 26,520인년, 계속 취업 약 29%, 재진입 약 36%, 복수 전환 약 27%의 분모·귀속을 확인했다.
- [x] 젠더 상호작용 `−0.02 (p < .001)`, `−0.03 (p < .05)`, `−0.38 (p < .01)`, `−0.28 (p < .01)`, `0.44 (p < .05)`의 부호·p값·귀속을 보존했다.
- [x] 여성의 이탈 오즈 약 39% 감소·42% 증가와 `ρ = 0.49`, 남성의 이탈 오즈 약 22% 감소와 `ρ = 0.44`를 해당 비교집단과 함께 확인했다.
- [x] `통계적으로 유의하지 않았다`, `강하게 관련되지 않았다`, `발견하지 못했다`, `~일 수 있다` 등 비유의·불확실성·연관성 범위를 보존하고 인과효과로 강화하지 않았다.
- [x] Tables 1–4 crop을 PDF 648–651쪽과 원해상도로 대조했다. crop 크기는 각각 980×986, 980×986, 980×1028, 980×1145픽셀이며 행·열·주석·유의성 기호가 잘리거나 빠진 곳은 없다.
- [x] Tables 1–2의 상태변화율·인년(`.20/.16/.25`, `10,414/6,528/3,886`; `.14/.13/.16`, `16,106/12,301/3,805`)을 확인했다.
- [x] Tables 3–4의 Models 1–3, 무작위효과 상관 `.49/.44`, `** p < .01`, `* p < .05`를 확인하고 캡션·접근성 설명과 네 이미지 대체텍스트를 번역했다.
- [x] 참고문헌 56개는 원문과 같은 11,449바이트이며 SHA-256 `B22AA61E9D28ECAA46786C19A36C1D9DC3F2F0CE7F62F84843764CFCB13065AB`로 byte-identical이다.
- [x] `check_alignment.js --strict`는 23/23, source-only 번역 스키마는 오류 0으로 통과했다. `--write-report`는 사용하지 않았다.
- [x] 최종 격리 전체 미리보기 `tmp/lee-yeung-stage2-full-preview-r4`에서 243 HTML / 5,713 로컬 대상, 117/117 문단 reveal, 372개 문장 reveal이 통과했다.
- [x] 수정된 경계형 italic 렌더러에서 `tr-041`·`tr-046`과 Tables 3–4의 `**`·`*`가 강조 태그로 삼켜지지 않고 문자 그대로 렌더됨을 확인했다.
- [x] 실제 브라우저에서 데스크톱 문장 툴팁·문단 전체 원문 펼치기와 390×844 모바일 문장 탭을 확인했다.
- [x] 번역 원천·파생 산출물 및 격리 렌더 결과에서 사적 경로, STT, 녹음·전사, 학생정보, 챗봇 표식은 0건이다.
- [x] 공개 `docs` 빌드, 승인, Stage 3 작성, 커밋은 수행하지 않았다.

## Manual review notes

- `tr-033`: 통계분석 원문의 한 문장이 한국어 두 문장으로 나뉘어, 의미와 숫자를 함께 덮는 결합 규칙을 사용했다.
- `tr-042`: Table 3 결과의 세미콜론 문장을 한국어 두 문장으로 나누되 계수·비교·유의성 범위를 한 쌍으로 유지했다.
- `tr-046`: Table 4 캡션의 Model 3 절을 한국어에서 분리했으나 원문 전체 절과 단조롭게 결합했다.
- `tr-048`: 원문의 인쇄상 붙임 `experiences.Net` 주변을 원문 수정 없이 경계 규칙으로 묶었다.
- `tr-060`: 연구비 문단의 저자 이니셜 `W-J. J. Yeung` 때문에 생긴 자동 분리 경계를 한 쌍으로 묶었다.
- source-only validator의 유일한 번역 경고는 직접 삽입된 표 이미지 4개 외에 표 캡션·설명 행도 세는 일반 라벨 휴리스틱이다. 네 이미지와 모든 캡션은 실제로 존재한다.
- 작성자 자체 검수에서는 해결되지 않은 내용 오류를 발견하지 못했지만, 작성자와 독립된 검수자의 전수 대조 전에는 승인하지 않는다.
